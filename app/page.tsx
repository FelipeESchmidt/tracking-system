import { Header } from "@/components/Header";
import styles from "./page.module.css";

export const Home = () => {
  return (
    <main className={styles.main}>
      <Header />
    </main>
  );
};

export default Home;
